import {
  userSchema,
  userSignInSchema,
  userIdSchemaParams,
  userIdSchemaBody,
} from './userSchema'
import { subjectSchema, subjectIdSchema } from './subjectSchema'
import { schoolSchema } from './schoolSchema'

export {
  userSchema,
  userSignInSchema,
  userIdSchemaParams,
  userIdSchemaBody,
  subjectSchema,
  subjectIdSchema,
  schoolSchema,
}
